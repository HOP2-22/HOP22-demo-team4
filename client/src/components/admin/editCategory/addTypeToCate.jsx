import { Button } from "@mui/material";
import { Box } from "@mui/material";

export default function AddTypeToCate() {
    
  return (
    <Box>
      <Button
        sx={{
          backgroundColor: "blue",
        }}
        variant="outlined"
      >
        Add category
      </Button>
    </Box>
  );
}
