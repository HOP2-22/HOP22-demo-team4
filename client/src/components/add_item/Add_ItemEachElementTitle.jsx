import React from "react";

const Add_ItemEachElementTitle = ({ className, children }) => {
  return <p className={`pt-5 text-[18px] ${className}`}>{children}</p>;
};

export default Add_ItemEachElementTitle;
